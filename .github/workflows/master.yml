name: bio_deploy

on:
  push:
    branches: [ master, Development ]
jobs:
  Make:
    runs-on: ubuntu-latest
    name: Deploy
    steps:
      - name: Checkout
        uses: actions/checkout@main
      - name: down
        uses: MitchWind/ftp-deploy@master
        with:
            server: "66.29.151.71"
            username: "bio"
            password: bio_diversity
            server_dir: "/bio-diversity"