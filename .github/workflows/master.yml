name: bio-deploy

on:
  push:
    branches: [ master ]
jobs:
  build:
    runs-on: ubuntu-latest
    name: Deploy
    steps:
      - name: Checkout & Deploy
        uses: actions/checkout@v2
      - run: yarn install
      - run: yarn build
      - run: yarn preDeploy
      - name: Ftp Deploy
        uses: SamKirkland/FTP-Deploy-Action@4.3.0
        with:
            server: 66.29.151.71
            username: root
            password: abab9I5E0H1i0SY3Zf
            local-dir: ./out/
            server-dir: /home/bio/bio-diversity/out/
      - run: yarn add node-fetch@2
      - run: node server/deploy.js